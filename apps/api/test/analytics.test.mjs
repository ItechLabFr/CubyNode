import assert from 'node:assert/strict';import test from 'node:test';import{aggregateMetricHistory,observedUptimePercent,percent}from'../src/analytics.mjs';test('percent handles zero totals',()=>{assert.equal(percent(10,0),0);assert.equal(percent(50,100),50)});test('history aggregates observed samples',()=>{const rows=[{collected_at:'2026-09-22T10:00:00Z',cpu_percent:10,memory_used_bytes:50,memory_total_bytes:100,storage_used_bytes:20,storage_total_bytes:100},{collected_at:'2026-09-22T10:00:05Z',cpu_percent:20,memory_used_bytes:60,memory_total_bytes:100,storage_used_bytes:30,storage_total_bytes:100}];const out=aggregateMetricHistory(rows,96);assert.ok(out.length>=1);assert.ok(out.every(p=>Number.isFinite(p.cpuPercent)))});test('uptime remains unknown without enough observations',()=>{assert.equal(observedUptimePercent([],5000),null);assert.equal(observedUptimePercent([{collected_at:'2026-09-22T10:00:00Z'}],5000),null)});